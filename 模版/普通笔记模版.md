---
描述:
排序:
分组:
分类: '<%* const f = tp.file.folder(true); const parts = f ? f.split("/").filter(s => s) : []; const base = parts.length ? parts[parts.length - 1] : ""; const hasMoc = base && tp.app.vault.getAbstractFileByPath(f + "/" + base + ".md"); tR += hasMoc ? "[[" + base + "]]" : ""; %>'
创建时间: <% tp.file.creation_date("YYYY年MM月DD日") %>
---
# <% tp.file.title %>

