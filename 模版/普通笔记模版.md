---
描述:
排序:
分组:
分类: '<%* const t = tp.file.title; const f = tp.file.folder(true); const parts = f ? f.split("/").filter(s => s) : []; let base, fp; if (parts.length && parts[parts.length - 1] === t) { const pp = parts.slice(0, -1); fp = pp.join("/"); base = pp.length ? pp[pp.length - 1] : ""; } else { fp = f; base = parts.length ? parts[parts.length - 1] : ""; } const hasMoc = base && tp.app.vault.getAbstractFileByPath((fp ? fp + "/" : "") + base + ".md"); tR += hasMoc ? "[[" + base + "]]" : ""; %>'
创建时间: <% tp.file.creation_date("YYYY年MM月DD日") %>
---
# <% tp.file.title %>

