---
分类:
  - "网页裁剪"
  - "[[类的更多内容]]"
标题: "类问题与练习（Java 官方教程）"
描述: "本 Java 入门教程描述 Java 编程语言的编程基础"
来源: "https://docs.oracle.com/javase/tutorial/java/javaOO/QandE/creating-questions.html"
发布者: "docs.oracle.com-Oracle"
发布时间:
创建时间: "2026-06-28T16:17:25+08:00"
---

Documentation

本 Java 教程是面向 JDK 8 编写的。本页所述的示例与实践并未利用后续版本中引入的改进，且可能使用了现已不再可用的技术。
如需利用最新版本改进的更新版教程，请参见 [Dev.java](https://dev.java/learn/)。
如需了解 Java SE 9 及后续各版本中语言特性的更新摘要，请参见 [Java 语言变更（Java Language Changes）](https://docs.oracle.com/pls/topic/lookup?ctx=en/java/javase&id=java_language_changes)。
如需了解所有 JDK 版本的新特性、增强功能以及被移除或弃用的选项，请参见 [JDK 发行说明（JDK Release Notes）](https://www.oracle.com/technetwork/java/javase/jdk-relnotes-index-2162236.html)。

## 类问题与练习

## 问题

1. 考虑下面的类：
	```java
	public class IdentifyMyParts {
	    public static int x = 7; 
	    public int y = 3; 
	}
	```
	1. 哪些是类变量？
		2. 哪些是实例变量？
		3. 下面代码的输出是什么：
		```java
		IdentifyMyParts a = new IdentifyMyParts();
		IdentifyMyParts b = new IdentifyMyParts();
		a.y = 5;
		b.y = 6;
		a.x = 1;
		b.x = 2;
		System.out.println("a.y = " + a.y);
		System.out.println("b.y = " + b.y);
		System.out.println("a.x = " + a.x);
		System.out.println("b.x = " + b.x);
		System.out.println("IdentifyMyParts.x = " + IdentifyMyParts.x);
		```

## 练习

1. 编写一个类，其实例表示一副扑克牌中的单张牌。扑克牌有两个区分属性：点数(rank)和花色(suit)。请务必保留您的解答，因为稍后会要求您在[枚举类型（Enum Types）](https://docs.oracle.com/javase/tutorial/java/javaOO/QandE/enum-questions.html)中改写它。
	---
	**提示：**
	您可以使用 `assert` 语句来检查您的赋值。编写方式为：
	```text
	assert (boolean expression to test);
	```
	如果布尔表达式为 false，您将得到一条错误信息。例如，
	```text
	assert toString(ACE) == "Ace";
	```
	应当返回 `true`，因此不会出现错误信息。
	如果您使用 `assert` 语句，必须使用 `ea` 标志运行您的程序：
	```text
	java -ea YourProgram.class
	```
	---
2. 编写一个类，其实例表示一**整副**扑克牌。您也应当保留这个解答。
3. 3. 编写一个小程序来测试您的牌组类和牌类。这个程序可以很简单，比如创建一副牌并显示其中的各张牌。

[检查您的答案。](https://docs.oracle.com/javase/tutorial/java/javaOO/QandE/creating-answers.html)
