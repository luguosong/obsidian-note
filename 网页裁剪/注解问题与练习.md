---
分类:
  - "网页裁剪"
  - "[[注解]]"
标题: "注解问题与练习（Questions and Exercises: Annotations）"
描述: "针对注解这一课的问题与练习"
来源: "https://docs.oracle.com/javase/tutorial/java/annotations/QandE/questions.html"
发布者: "docs.oracle.com-Oracle"
发布时间:
创建时间: "2026-06-28T19:28:23+08:00"
---

## 问题与练习：注解

## 问题

1. 下面这个接口有什么问题？
	```java
	public interface House {
	    @Deprecated
	    void open();
	    void openFrontDoor();
	    void openBackDoor();
	}
	```
2. 考虑问题 1 中 `House` 接口的如下实现。
	```java
	public class MyHouse implements House {
	    public void open() {}
	    public void openFrontDoor() {}
	    public void openBackDoor() {}
	}
	```
	如果你编译这个程序，编译器会产生一个警告，因为 `open` 已（在接口中）被弃用。你能做些什么来消除该警告？
3. 下面的代码能否无错误地编译？为什么能或为什么不能？
	```java
	public @interface Meal { ... }
	@Meal("breakfast", mainDish="cereal")
	@Meal("lunch", mainDish="pizza")
	@Meal("dinner", mainDish="salad")
	public void evaluateDiet() { ... }
	```

## 练习

1. 为一个功能增强请求定义一个注解类型，其元素包括 `id`、`synopsis`、`engineer` 和 `date`。将 `engineer` 的默认值指定为 `unassigned`，将 `date` 的默认值指定为 `unknown`。

[查看答案。](https://docs.oracle.com/javase/tutorial/java/annotations/QandE/answers.html)
