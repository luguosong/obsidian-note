---
status: active
---

# 进入单 skill 深挖阶段（Phase 2），首站 setup-matt-pocock-skills

2026-07-20 用户明确"这次详细学习每一个 skill，先从 setup-matt-pocock-skills 开始"——课程从"全局地图 / 工作流实战"进入**逐个 skill 深挖**阶段（MISSION 此前列为"后续课"，现转为进行中）。lesson 3（原 lesson 4，后于重构顺延）已交付：把 setup 的核心讲成**间接层**（skills 写抽象角色、setup 把角色绑定到本仓现实），并锚定用户自己仓库已存在的 `docs/agents/` 配置当活教材。

**Implications：**
- 后续按用户点单逐个深挖；每课复用 kami 样式 + `quiz.js`，锚定用户真实仓库/项目优先于虚构示例。
- **用户仓库 = 已完成 setup 的样本**（GitHub tracker / PRs=no / 默认 5 标签 / single-context / 根用 `CLAUDE.md`），可作后续 triage、to-tickets、wayfinder 等工程 skill 课的共同真实底座。
