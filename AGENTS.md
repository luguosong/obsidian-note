# AGENTS.md — Obsidian 笔记仓库协作约定

本仓库是 **Obsidian 笔记库**（vault），不是软件项目：内容为个人知识管理与学习笔记，agent 的工作以**创建、编辑、整理笔记**为主。全程中文沟通，中文撰写笔记与提交信息。

## 仓库结构

```
obsidian-note/
├── 01-编程笔记/            # 编程类正式笔记（内部主题目录无编号，见「笔记分类机制」第 5 点）
│   ├── Java/               # Java（JavaSE、JVM 等）
│   ├── Ai/                 # AI / 编程智能体
│   ├── 前端/               # 前端工程
│   ├── 密码学/             # 国密 / PKI
│   ├── 电子印章管理平台/    # 电子签章专题
│   ├── Obsidian/           # Obsidian 使用、插件、markdown 语法
│   └── …                   # Docker/Git/Linux/Mysql/Redis/Windows 等其余主题目录同此
├── 02-数学/ ~ 11-其它/     # 其余学科分区（数学/英语/经济学/历史/物理/化学/地理/生物/文学/其它）
├── 未分类笔记/             # 收件箱：临时想法与零散知识点（暂存区，非正式分类）
├── 网页裁剪/               # 从网页抓取/裁剪保存的内容
├── 数据库/                 # .base 聚合索引（含共享 分类索引.base，见「Base 目录索引」）
├── 附件/                   # 图片、PDF 等资源
├── 模版/                   # Templater 笔记模版
├── 待办任务/               # 任务清单（见 .gitignore，不纳入版本控制）
├── code/                   # 与笔记配套的代码示例（见「`code/` 代码示例目录」）
├── docs/                   # Agent / 工程约定文档（issue-tracker、triage 等）
└── .obsidian/              # Obsidian 配置、插件、主题（纳入版本控制）
```

- **笔记落点**：正式、规整的笔记放对应**学科分区**（编程→`01-编程笔记/`，数学→`02-数学/`，依此类推）；临时想法、零散知识点先放 `未分类笔记/` 收件箱，主题清晰后再迁入正式分区。⚠️ **收件箱只收「还不确定归哪」的临时笔记**——已有明确归属的成熟笔记直接进所属分类目录；收件箱一旦混入成品笔记就失去「待整理」信号，分类目录也变空壳。归属不明时默认走收件箱，拿不准再问用户。
- **teach 课程产物（HTML）不放本仓库**：课程独立维护于 WSL 仓库 `~/code/personal/learning`（`\\wsl.localhost\Ubuntu\home\luguosong\code\personal\learning`，独立 git，约定见其 `README.md`）。笔记是手动整理的最终成品，课程是与 agent 交流的过程产物，两者互不混放；课程 HTML 不进学科分区、不参与双链。

## 笔记分类机制（核心约定）

`01-编程笔记/` 采用**「目录即分类」**（配合 Notebook Navigator 插件按文件夹分组显示）：**笔记放在哪个目录，就属于哪个分类**。四个机制各司其职、不要混用：

> **目录管「放哪」（即分类），`分类` 属性管「向上归属」，Base 视图管「向下索引」，正文 wikilink 管「和谁有关系」**。

1. **分类 = 目录**：`01-编程笔记/` 下每个主题子目录（`Java/`、`Ai/`、`Docker/`、`前端/` 等）就是一个分类——`01-编程笔记/Java/JVM.md` 属于 Java 分类。
2. **文件夹笔记当 MOC/入口页**：每个分类目录都有一张**与目录同名**的笔记（如 `Java/Java.md`）作为 MOC。MOC 正文在 `# 标题` 后嵌入 `![[数据库/分类索引.base]]`，**动态**列出本目录直接子内容（直接叶子笔记 + 直接子目录的文件夹笔记），新笔记自动出现——**不手工往 MOC 正文维护 `- [[笔记]]` 索引列表**（已废弃的旧做法）。空分类目录也建占位 MOC（frontmatter + 一级标题 + 一句话主题说明 + 嵌入）；新增分类目录时建好同名占位 MOC 即完成，无其它维护。文件夹笔记在列表中隐藏、点目录即打开（Notebook Navigator `enableFolderNotes: true` / `hideFolderNoteInList: true`）；新目录的图标/颜色需手动在 Notebook Navigator 配置。**分类仍由目录决定，文件夹笔记只是导航件，不是分类依据**。
3. **笔记间的关联用正文 wikilink**：语义关联（如 `JavaSE.md` 用 `- [[Java概述]]` 串起子主题）写在**正文**里，红链当面包屑；没有 `关联笔记` 字段。
4. **frontmatter `分类` 字段承载向上归属**：每篇笔记（**含文件夹笔记自身**）都有 `分类: "[[直接父目录的文件夹笔记]]"` 属性（类型「链接」），每层一跳，整棵分类树由清一色的向上单跳边构成。⚠️ **值必须带引号**：裸写 `分类: [[X]]` 会被 YAML 解析成嵌套列表而不是链接属性，反链/图谱不生效。**顶层分区 MOC**（`01-编程笔记.md`、`02-数学.md` …）与**无文件夹笔记的目录**（`未分类笔记/`、`附件/` 等）里的笔记 `分类` 留空；新建笔记由模版自动填充（见「Frontmatter」）。⚠️ **手动移动笔记后 `分类` 不会自动更新**：它是静态文本，移动后需改为**新父目录**的文件夹笔记（agent 执行移动时自动跟着改；Base 视图按目录过滤、自动跟随）。
5. **顶层分区带编号，内部主题目录不带**：顶层分区用「`编号-名称`」（`01-编程笔记` … `11-其它`）控制显示顺序；`01-编程笔记/` 内部主题子目录直接用主题名（`Java`、`Ai`、`计算机组成原理`），**按字母序显示**——Notebook Navigator 不支持子目录手动排序（仅仓库根级目录支持拖拽手排，存于 `rootFolderOrder`）。某主题确需置顶/自定义顺序时，唯一杠杆是给目录名加数字前缀（默认不加，接受字母序）。

## Base 目录索引

- 视图为**列表**布局，按 `排序` 属性**升序**排列（有值的在前、无值的殿后），列表项显示：笔记名 / `描述` / `创建时间`。调整展示列或排序，改 `数据库/分类索引.base` 的 `views` 段（`order` 控制显示列，`sort` 控制排序）。
- 共享模板的 filter 以 `this`（嵌入时的宿主笔记）过滤：直接叶子（`file.folder == this.file.folder`）∪ 深度恰 +1 的子目录文件夹笔记（`file.inFolder(...)` 且 `file.folder.endsWith(file.basename)`），并排除宿主自身。完整逻辑以 `数据库/分类索引.base` 文件本身为准。

## 笔记编写规范

### Frontmatter（YAML）

每篇笔记以 frontmatter 开头，`01-编程笔记/` 下统一以 `模版/普通笔记模版.md` 为基础模版：

```yaml
---
描述:                    # 一句话说明这篇笔记讲什么
排序:                    # 数字，供 Notebook Navigator 在目录内手动排序（manualSortPropertyKey）
分组:                    # 可选分组标识（预留字段，目前未启用）
分类:                    # "[[直接父目录的文件夹笔记]]"，必须带引号（见「笔记分类机制」第 4 点；模版自动填充）
创建时间: 2026年06月24日
---
```

- **创建时间**：Templater 应用模版时自动生成（`tp.file.creation_date("YYYY年MM月DD日")`），格式统一中文 `YYYY年MM月DD日`（精确到日）；手动新建时填当前日期，不回填历史时间。
- **不设「修改时间」字段**：Templater 是一次性替换机制，该字段只会写死成创建瞬间的普通文本、之后不随编辑更新（真实修改时间看文件信息面板），留着只是无效值。
- 按需扩展的额外字段（如 `来源`、`更新时间`）可自行添加并保留；基础字段未用可留空，但保留不删，保持结构一致。

### 正文

- 首行为一级标题：`# 笔记标题`（与文件名一致）。
- 优先使用 **Obsidian 风格语法**：
	- 内部链接用 **wikilink**：`[[笔记名]]`、`[[笔记名|显示文本]]`、`![[笔记名]]`（嵌入）。
	- 外部链接用标准 Markdown：`[文本](https://...)`。
	- 高亮用 `==文本==`。
- 代码块标注语言：` ```java `、` ```js `、` ```bash ` 等。
- 图片引用：`![[图片名.png]]`，图片文件放在 `附件/`。

### 标题结构（TOC 即复习提纲）

标题不只是排版层级——**TOC（标题大纲）相当于这篇笔记的思维导图，是用户的核心复习工具**：复习时只扫一遍标题，就应能回忆起全篇重要知识点的细节脉络，再按标题下钻正文。标题粒度按此标准取舍：

- **知识点、疑难点、易错点、对比辨析升为标题**：标题直接点名知识点本身，疑问句式尤佳（如「为什么不用 X 代替 Y」这类辨析问题），把复习时最想被唤起的问题直接写进 TOC。
- **细枝末节留在正文**：普通陈述、代码讲解、过渡说明用加粗/列表承载即可。细枝末节也标题化，TOC 会沦为噪音，反而淹没真正要复习的重点。
- 自检：**只看 TOC，能否回忆起这篇笔记讲了什么、难点在哪**——能，则粒度合适；满眼琐碎标题或关键点缺席，则需调整。

### 相关资源

- 一个分类的「相关资源」（外链、参考书、关联文章等）直接写进该分类**文件夹笔记 MOC** 的 `## 相关资源` 段（如 `01-编程笔记/Obsidian/Obsidian.md`），**不单开 `相关资源.md` 笔记**。
- 只有当资源内容本身已是**成稿知识笔记**（如系统综述）时，才独立成篇并由 MOC 链接。

## 网页裁剪约定

- `defuddle` skill 抓取的网页内容统一放 `网页裁剪/`；`01-编程笔记/` 只写自己的整理与理解，引用裁剪内容用 **wikilink 双链**（如 `[[ClaudeCode在大型代码库中的工作方式：最佳实践与入门]]`），不整段复制原文。
- ⚠️ **`分类` 字段两种语义，不混用**：defuddle 裁剪笔记是**列表格式**（`分类: ["网页裁剪"]`，可追加 `"[[父页面名]]"`），表达来源/父子层级，存量保持原样、不迁移；分类目录笔记一律是**单链接** `分类: "[[父MOC]]"`，表达向上归属。

## `code/` 代码示例目录

`code/` 存放与笔记配套的**可运行代码示例**（中文笔记 ↔ 英文代码，命名规则截然不同）：目录树与 `01-编程笔记/` 逐级同构，命名全英文 kebab-case（`Java/`→`code/java/`、`前端/`→`code/frontend/`，项目建议 `-demo` 后缀）。**在 `code/` 下创建/修改代码，或笔记要用 `--8<--` 引用 `code/` 源文件时，先读 `docs/agents/code-examples.md`**（完整约定：目录对应、`--8<--` 写法、JDK 21 / pnpm 默认技术栈）。

## 命名约定

- 笔记文件名用中文，简短有意义，无日期前缀（除非确属日志类）。
- 目录编号规则见「笔记分类机制」第 5 点：顶层分区「编号-名称」，`01-编程笔记/` 内部主题目录纯主题名、按字母序。
- **目录深度跟随主题，不设硬上限**（从 `01-编程笔记/` 算起）：更细的主题优先用「子目录」表达——子目录由父 MOC 的 Base 视图自动列出，无需补链；只在主题确实需要独立目录时才开新层，别为凑层数把自然细分拍扁、也别为统一把需要独立目录的主题硬塞进一篇笔记。

## Git 提交约定

- 自动备份提交 `vault backup: YYYY-MM-DD HH:mm:ss` 由 obsidian-git 插件生成，**不要手动模拟该格式**。
- **手动提交**用 Conventional Commits 中文描述：`feat: 新增 Claude Code 学习笔记`、`chore: 更新插件配置`、`docs: 完善模版说明`。
- 仅在用户明确要求时执行 `git add` / `commit` / `push`；编译产物、依赖、IDE 文件已被 `.gitignore` 忽略（含 `待办任务/`、`node_modules/`、`*.iml`），只提交源码与必要配置。

## 禁止触碰的内容

- **`.obsidian/workspace.json`**（本地窗口布局，频繁变动）。
- 擅自删除已有笔记或重构目录结构——涉及整理、迁移先与用户确认。
- `.obsidian/plugins/` 下插件的内部实现，除非用户明确要求处理插件配置。

## 工作偏好

- 涉及创建/编辑 `.md`、`.base`、`.canvas` 等 Obsidian 文件时，优先调用对应 skill（`obsidian-markdown`、`obsidian-bases`、`obsidian-canvas-creator`、`mermaid-visualizer` 等）；配图优先用 Mermaid（Obsidian 原生渲染）。
- 提供网页 URL 时，优先用 `defuddle` skill 提取干净内容再整理入库。
- 不确定分类或位置时，默认按上文「笔记落点」处理（收件箱兜底），拿不准先问用户。

## Agent skills

> 为 Matt Pocock 工程 skills（`mattpocock-skills` 插件，名单以 v1.2.3 为准：triage / to-tickets / to-spec / tdd / code-review / diagnosing-bugs / improve-codebase-architecture / domain-modeling / grill-with-docs / wayfinder 等）提供仓库级配置。详细约定见 `docs/agents/`。

### Issue tracker

Issues 存放在 GitHub（仓库 `luguosong/obsidian-note`），用 `gh` CLI 读写；外部 PR **不**作为 triage 请求入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个 triage 角色直接用默认标签名（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：仓库根一份 `CONTEXT.md` + `docs/adr/`（暂未创建，按需由 `/domain-modeling` 生成）。详见 `docs/agents/domain.md`。
